name = "seekmoon/native_subprocess_clap_probe"

version = "0.1.0"

readme = "README.md"

license = "Apache-2.0"

keywords = ["moonbit", "native", "subprocess", "jq", "cli", "clap"]

description = "MoonBit native subprocess and clap-style CLI parser probe."

import {
  "moonbitlang/async@0.19.4",
  "TheWaWaR/clap@0.2.6",
}

preferred_target = "native"

supported_targets = "native"
