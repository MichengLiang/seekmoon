name = "seekmoon/native_subprocess_cli_probe"

version = "0.1.0"

readme = "README.md"

license = "Apache-2.0"

keywords = ["moonbit", "native", "subprocess", "jq", "cli"]

description = "MoonBit native subprocess probe that delegates JSON filtering to host jq."

import {
  "moonbitlang/async@0.19.4",
}

preferred_target = "native"

supported_targets = "native"
