name = "seekmoon/mooncakes_http_reader_probe"

version = "0.1.0"

readme = "README.md"

license = "Apache-2.0"

keywords = ["moonbit", "native", "http", "mooncakes", "reader", "mio"]

description = "MoonBit native HTTP reader probe for Mooncakes API, asset, and source zip endpoints."

import {
  "moonbitlang/async@0.19.4",
  "oboard/mio@0.5.2",
}

preferred_target = "native"

supported_targets = "native"
